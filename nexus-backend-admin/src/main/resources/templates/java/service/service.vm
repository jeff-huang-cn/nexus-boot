package ${packageName}.service.${businessName};

import ${packageName}.controller.${businessName}.vo.*;
import ${packageName}.dal.dataobject.${businessName}.${className}DO;
#if($templateType == 3)
import ${packageName}.dal.dataobject.${businessName}.${subTable.className}DO;
#end
#if($templateType != 2)
import com.nexus.framework.web.result.PageResult;
#end
import jakarta.validation.Valid;
import java.util.List;

/**
 * ${classComment} Service 接口
 *
 * @author ${author}
 * @since ${date}
 */
public interface ${className}Service {

    /**
     * 创建${classComment}
     *
     * @param createReqVO 创建信息
     * @return 编号
     */
    Long create(@Valid ${className}SaveReqVO createReqVO);

    /**
     * 更新${classComment}
     *
     * @param updateReqVO 更新信息
     */
    void update(@Valid ${className}SaveReqVO updateReqVO);

    /**
     * 删除${classComment}
     *
     * @param id 编号
     */
    void delete(Long id);
#if($templateType == 1 || $templateType == 3)

    /**
     * 批量创建${classComment}
     *
     * @param createReqVOs 创建信息列表
     */
    void batchCreate(List<${className}SaveReqVO> createReqVOs);

    /**
     * 批量更新${classComment}
     *
     * @param updateReqVOs 更新信息列表
     */
    void batchUpdate(List<${className}SaveReqVO> updateReqVOs);

    /**
     * 批量删除${classComment}
     *
     * @param ids 编号列表
     */
    void batchDelete(List<Long> ids);
#end

    /**
     * 获得${classComment}详情
     *
     * @param id 编号
     * @return ${classComment}详情
     */
    ${className}DO getById(Long id);
#if($templateType == 1 || $templateType == 3)

    /**
     * 获得${classComment}分页
     *
     * @param pageReqVO 分页查询
     * @return ${classComment}分页
     */
    PageResult<${className}RespVO> getPage(${className}PageReqVO pageReqVO);

    /**
     * 获得${classComment}列表（用于导出）
     *
     * @param pageReqVO 查询条件
     * @return ${classComment}列表
     */
    List<${className}DO> getList(${className}PageReqVO pageReqVO);
#end
#if($templateType == 1)

#end
#if($templateType == 2)

    /**
     * 获得${classComment}列表（树形结构）
     *
     * @param listReqVO 查询条件
     * @return ${classComment}列表
     */
    List<${className}RespVO> getList(${className}ListReqVO listReqVO);
#end
#if($templateType == 3)

    // ==================== 子表（${subTable.classComment}） ====================

    /**
     * 获得${subTable.classComment}列表
     *
     * @param ${subJoinColumn.javaField} ${subJoinColumn.columnComment}
     * @return ${subTable.classComment}列表
     */
    List<${subTable.className}DO> get${subTable.className}ListBy${subJoinColumn.javaField.substring(0,1).toUpperCase()}${subJoinColumn.javaField.substring(1)}(Long ${subJoinColumn.javaField});
#end
}

