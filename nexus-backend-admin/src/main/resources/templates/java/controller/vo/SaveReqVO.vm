package ${packageName}.controller.${businessName}.vo;

import com.alibaba.excel.annotation.ExcelProperty;
import com.fasterxml.jackson.annotation.JsonFormat;
import io.swagger.v3.oas.annotations.media.Schema;
#if($templateType == 3)
import jakarta.validation.Valid;
import jakarta.validation.constraints.NotEmpty;
#end
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import lombok.Data;
#if($hasLocalDateTime)
import java.time.LocalDateTime;
#end
#if($hasBigDecimal)
import java.math.BigDecimal;
#end
#if($templateType == 3)
import java.util.List;
#end

/**
 * ${classComment} 新增/修改 Request VO
 *
 * @author ${author}
 * @since ${date}
 */
@Schema(description = "${classComment} 新增/修改 Request VO")
@Data
public class ${className}SaveReqVO {

    @Schema(description = "ID", example = "1")
    private Long id;

#foreach ($column in $columns)
#if(!$column.primaryKey && ($column.createOperation || $column.updateOperation))
    @Schema(description = "${column.columnComment}"#if(!$column.nullable), requiredMode = Schema.RequiredMode.REQUIRED#end, example = "${column.example}")
    @ExcelProperty("${column.columnComment}")
#if(!$column.nullable)
#if($column.javaType == "String")
    @NotBlank(message = "${column.columnComment}不能为空")
#else
    @NotNull(message = "${column.columnComment}不能为空")
#end
#end
#if($column.javaType == "LocalDateTime")
    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ss")
#end
    private ${column.javaType} ${column.javaField};

#end
#end
#if($templateType == 3)
    @Schema(description = "${subTable.classComment}列表")
    @Valid // 开启嵌套校验
    @NotEmpty(message = "${subTable.classComment}列表不能为空")
    private List<${subTable.className}SaveReqVO> ${subTable.classNameFirstLower}s;
#end
}

